@page "{studentId:int?}"
@model StudentDataApp.Pages.PostGradPage.CreateModel

@{
    ViewData["Title"] = "Create";
    ViewData["StudentID"] = Model.StudentID;
}

<h1>Create</h1>

<h4>Post-Graduation</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="PostGrad.StudentID" value=@Model.StudentID />
            <div class="form-group">
                <label asp-for="PostGrad.GradCode" class="control-label"></label>
                <select asp-for="PostGrad.GradCode" asp-items= "@Model.GradCodes" class="form-control"></select>
                <span asp-validation-for="PostGrad.GradCode" class="text-danger" />
            </div>
            <div class="form-group">
                <label asp-for="PostGrad.GradTerm" class="control-label"></label>
                <input asp-for="PostGrad.GradTerm" class="form-control" />
                <span asp-validation-for="PostGrad.GradTerm" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="PostGrad.CompName" class="control-label"></label>
                <input asp-for="PostGrad.CompName" class="form-control" />
                <span asp-validation-for="PostGrad.CompName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="PostGrad.CompLocation" class="control-label"></label>
                <input asp-for="PostGrad.CompLocation" class="form-control" />
                <span asp-validation-for="PostGrad.CompLocation" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="PostGrad.Salary" class="control-label"></label>
                <input asp-for="PostGrad.Salary" class="form-control" />
                <span asp-validation-for="PostGrad.Salary" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
